#If you omit __init__.py, Python may not recognize the directory as a package, and you won't be able to import modules from it.
# Example: from app import routes
# you cannot import routes.py if __init__.py is missing in the same directory.